{% include 'header' %}
@protocol {{ prefix }}{{ module_info.name }}ViewActionDelegate <NSObject>

@end

@interface {{ prefix }}{{ module_info.name }}View : UIView

@property (weak, nonatomic) IBOutlet id<{{ prefix }}{{ module_info.name }}ViewActionDelegate> actionDelegate;

@end
